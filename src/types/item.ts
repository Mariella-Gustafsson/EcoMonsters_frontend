export type ItemsAPI = {
  name: string;
  id: number;
  image_url: string;
  correct_material: string;
};

export type Item = {
  name: string;
  id: number;
  image: string;
  materialType: string;
};
